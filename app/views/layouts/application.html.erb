<!DOCTYPE html>
<html>
<head>
  <title>Scheduler</title>
  <script src="http://code.jquery.com/jquery.js"></script>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
	<div class="navbar navbar-inverse navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container">
        <button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to "Schedule Miner v2.0", home_url, { :class => "brand"} %>

        <ul class="pull-right nav">
        <% if session[:user_id] == nil %>
      		<li><%= link_to 'Sign In', login_path %></li>
      		<li><%= link_to 'Create New Account', new_user_path %></li>
        <% else %>
          <li class="dropdown">
            <a class="dropdown-toggle" data-toggle="dropdown" href="">Pages<b class="caret"></b></a>
            <ul class="dropdown-menu">
              <li><%= link_to 'Users', users_url %></li>
              <li><%= link_to 'Subjects', subjects_url %></li>
              <li><%= link_to 'Courses', courses_url %></li>
              <li><%= link_to 'Instructors', instructors_url %></li>
            </ul>
          </li>
          <li><%= link_to 'Sign Out', logout_path, :method => :delete %></li>
          <li><%= link_to User.find_by_id(session[:user_id]).name, edit_user_path(session[:user_id]) %></li>
          <li>
            <% if User.find_by_id(session[:user_id]).photo.exists? %>
            <%= image_tag User.find_by_id(session[:user_id]).photo(:thumb) %>
            <% else %>
            <%= image_tag 'user_default.jpg' %>
            <% end %>
          </li>
        <% end %>
        </ul>
        <div class="nav-collapse collapse">
          <ul class="nav">
            <li class="active"><a href="#">Home</a></li>
            <li><a href="#about">About</a></li>
            <li><a href="#contact">Contact</a></li>
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </div>
	</div>

	<div class="container">
		<%= yield %>
	</div> <!-- container -->

</body>
</html>
