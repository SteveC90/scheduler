<h1>Editing instructor</h1>
<% if (session[:user_id] && User.find_by_id(session[:user_id]).role != User::ROLES[0]) %>
<%= render 'form' %>
<% else %>
<p id="notice">You do not have permission to edit an instructor.</p>
<% end %>
